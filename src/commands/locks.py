import asyncio

leaderboard_lock = asyncio.Lock()
average_lock = asyncio.Lock()
import_lock = asyncio.Lock()
match_lock = asyncio.Lock()
line_lock = asyncio.Lock()
tens_lock = asyncio.Lock()
big_lock = asyncio.Lock()
